<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>I Have Something - Valentine's Mail</title>
    <link rel="shortcut icon" href="images/gift.png" type="image/x-icon">
    <link rel="stylesheet" href="css/index.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Global Styles */
        body {
            background: linear-gradient(270deg, #ff6b81, #a65eaa);
            background-size: 400% 400%;
            animation: gradientAnimation 15s ease infinite;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        @keyframes gradientAnimation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        /* Envelope Styles */
        .envelope {
            width: 400px;
            height: 250px;
            position: relative;
            perspective: 1000px;
            cursor: pointer;
            margin: 0 auto; /* Center the envelope */
        }

        .envelope-inner {
            width: 100%;
            height: 100%;
            background-color: #ffffff;
            border-radius: 10px;
            position: relative;
            transition: transform 0.6s;
            transform-style: preserve-3d;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }

        .envelope-flap {
            position: absolute;
            width: 100%;
            height: 50%;
            background-color: #ff6b81;
            clip-path: polygon(0 0, 100% 0, 50% 100%); /* Flap points downward */
            top: 0; /* Positioning the flap at the top */
            left: 0;
            transition: transform 0.6s; /* Add smooth transition */
            transform-origin: top;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            z-index: 1; /* Ensure flap is above the inner envelope */
        }

        /* This class is toggled when the envelope is opened */
        .envelope.open .envelope-flap {
            transform: rotateX(-60deg); /* Rotate the flap up, less than 90deg */
            z-index: 0; /* Send flap behind the letter */
        }

        .envelope.open .envelope-inner {
            transform: rotateY(0deg);
        }

        /* Letter Styles */
        .letter {
            width: 350px;
            height: 200px;
            background-color: #ffffff;
            border: 2px solid #ff6b81;
            border-radius: 10px;
            position: absolute;
            bottom: -220px; /* Initially hidden below the envelope */
            left: 50%;
            transform: translateX(-50%);
            padding: 10px;
            opacity: 0; /* Initially hidden */
            transition: bottom 0.5s ease, opacity 0.5s ease;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .letter-content {
            text-align: center;
        }

        /* Button Styles */
        .btn {
            display: inline-block;
            padding: 10px 20px;
            margin-top: 20px;
            background-color: #ff6b81;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn:hover {
            background-color: #ff4d6b;
        }

        /* Letter Text Styles */
        .letter-content h2 {
            margin: 0;
            font-size: 24px;
        }

        .letter-content p {
            margin: 5px 0;
            font-size: 16px;
        }
    </style>
</head>

<body>
    <!-- Main Content -->
    <div class="envelope" onclick="openEnvelope(this)">
        <div class="envelope-inner">
            <div class="envelope-flap"></div>
            <div class="letter" id="letter">
                <div class="letter-content">
                    <h2>Happy Valentine's Day!</h2>
                    <p>Sending you all my love on this special day. 💌</p>
                    <p>May your day be filled with joy and happiness!</p>
                    <a href="<%= flower_gifts_path %>" class="btn">Open Gift</a>
                </div>
            </div>
        </div>
    </div>

    <script>
        function openEnvelope(envelope) {
            const letter = document.getElementById('letter');
            envelope.classList.toggle('open'); // Toggle the open class on the envelope
            if (envelope.classList.contains('open')) {
                letter.style.bottom = '20px'; // Raise the letter into view
                letter.style.opacity = '1'; // Make the letter visible
            } else {
                letter.style.bottom = '-220px'; // Hide the letter
                letter.style.opacity = '0'; // Make the letter invisible
            }
        }
    </script>
</body>

</html>
